/* Nota Bene: per semplicita' sono state tolte
   le righe corrispondenti agli include
*/



int main(void) {

	if(1)
		printf("ciao "); printf("vaffa ");
	else
		printf( "non saluta\n" );

	printf( "\nfine\n" );
	return(0);
}
