/* Nota Bene: per semplicita' sono state tolte
   le righe corrispondenti agli include
*/

int main(void) {

	if(1)
		do { printf("ciao "); printf("vaffa "); } while(0);
	else
		printf( "non saluta\n" );

		printf( "\nfine\n" );
		return(0);
}

